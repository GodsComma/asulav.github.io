// import useTheme  from "hooks/useTheme";
// import React, { createContext, MouseEventHandler, ReactEventHandler, useContext, useState } from "react";


// interface DropdownProps {
//     children?: any;
//     hidden?: boolean;
//     onChange?: React.Dispatch<any>;
//     OnClick?: React.Dispatch<any>;
//     value?: any
// }

// interface DropdownItemProps {
//     children?: any;
//     handleClick: React.ChangeEvent<HTMLInputElement> | any
//     value: string | object | boolean | null;
// }

// interface DropdownContextProps {
//     hidden: boolean,
//     handleClick: React.ChangeEvent<HTMLInputElement> | any
// }


// const initialState: DropdownContextProps = {
//     hidden : false,
//     handleClick : null
// }

// const DropdownContext = createContext(initialState);

// const DropdownHeader = ({ children } : DropdownProps) => {
//     const { theme } = useTheme();

//     return (
//         <div className={` ${theme['button-p-hover']} cursor-pointer inline-flex rounded-md px-4 py-2 border-stone-800 border-1 shadow shadow-black items-center`}>
//             { children }
//         </div>
//     )
// }

// const DropdownItem = ({ children, value, handleClick }: DropdownItemProps) => {

//     if(children == null){
//         children = value;
//     }

//     const onclick = (Event: React.Dispatch<any>) => {
//         console.log('Click');
//     }

//     return (
//         <div className="container border-double border-white border-2 max-w-xs px-2 cursor-pointer" onclick={onclick}> {children as String} </div>
//     )
// }

// const DropdownGroup = ({ children }: DropdownProps ) => {

//     const hidden = useContext(DropdownContext)

//     return (
//         <div className={` ${hidden ? 'hidden' : ''} container grid grid-col-1 mx-auto`}>
//             { children }
//         </div>
//     )
// }


// const Dropdown = ({ children, value } : DropdownProps) => {
//     const [ hidden, sethidden ] = useState(true);

//     const showDropdown = () => {
//         sethidden(false);
//     }

//     const hideDropdown = () => {
//         sethidden(true);
//     }

//     const handleClick = ( event: React.ChangeEvent<HTMLInputElement> | null | any ) => {
//         value = event?.target.value;
//     }

//     return (
//         <DropdownContext.Provider value={ { hidden, handleClick }  }>
//             <div className="container px-2 py-2" onMouseEnter={showDropdown} onMouseLeave={hideDropdown}>
//                 { children }
//             </div>
//         </DropdownContext.Provider>
            
//     );
// }

// Dropdown.Header = DropdownHeader;
// Dropdown.Item = DropdownItem;
// Dropdown.Group = DropdownGroup;

// export default Dropdown;